version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
      - SECRET_KEY=dev-secret-key
      # Add your GEMINI_API_KEY here or use .env file
    volumes:
      - .:/app
    command: flask run --host=0.0.0.0
    env_file:
      - .env
